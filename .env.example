# Optional fallbacks if you prefer env vars over the in-app key form.
# The app stores the key locally in .northline/credentials.json instead.

# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENROUTER_API_KEY=
# NORTHLINE_MODEL=
# NORTHLINE_FAST_MODEL=

# SEC fair-access User-Agent (recommended)
# SEC_USER_AGENT=Northline Finance research/1.0 (your@email)
